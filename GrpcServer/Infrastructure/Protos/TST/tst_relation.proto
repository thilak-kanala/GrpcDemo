syntax = "proto3";

option csharp_namespace = "GrpcServer.Protos.TST";

package tst;

import "TST/tst_user.proto";
import "TST/tst_group.proto";
import "Common/base_relation.proto";

// Response message for GetUserGroups
message GetUserGroupsResponse {
  repeated TstGroupMessage groups = 1;
}

// Response message for GetGroupUsers
message GetGroupUsersResponse {
  repeated TstUserMessage users = 1;
}

// Service definition for TST User-Group relationship operations
// Provides operations to manage many-to-many relationships between users and groups
service TstUserGroupRelationGrpcServiceDefinition {
  // Retrieves all groups that a specific user belongs to
  rpc GetUserGroups (common.GetUserGroupsRequest) returns (GetUserGroupsResponse);
  
  // Adds a user to multiple groups
  rpc AddUserToGroups (common.AddUserToGroupsRequest) returns (common.AddUserToGroupsResponse);
  
  // Removes a user from a specific group
  rpc RemoveUserFromGroup (common.RemoveUserFromGroupRequest) returns (common.RemoveUserFromGroupResponse);
  
  // Retrieves all users that belong to a specific group
  rpc GetGroupUsers (common.GetGroupUsersRequest) returns (GetGroupUsersResponse);
  
  // Adds multiple users to a specific group
  rpc AddUsersToGroup (common.AddUsersToGroupRequest) returns (common.AddUsersToGroupResponse);
}

